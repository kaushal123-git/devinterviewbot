# utils/

Miscellaneous helpers and utilities.

Files:

- `audioUtils.ts` — Utility helpers for audio processing, conversions, sample-rate handling, and possibly small helpers used by the voice hooks.

Guidelines:
- Keep pure utility functions here and avoid side effects; use hooks for anything that needs lifecycle management.